VITE_API_URL=http://localhost:8000/api/v1

# local keeps access/refresh tokens in localStorage for local demo convenience.
# cookie keeps the access token in memory and uses the HttpOnly refresh cookie.
VITE_AUTH_STORAGE_MODE=local
